version: "3.9"
services:
  web:
    build: .
    networks:
      - ts-at
  redis:
    image: redis
    networks:
      - ts-at
  mongo:
    image: mongo
    networks:
      - ts-at
networks:
  task-scheduler-at:
    name: ts-at
